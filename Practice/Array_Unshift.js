// Unshift adds element in begining
let arr = [1, 2, 3, 4,];
// let arr = [1, 2, 3, 4];    Both are same
arr.unshift( -1, 0);  // Either we can single argument or multiple argument
// arr.unshift( -2);
console.log(arr);